# Start Here

For a fresh Codex session:

1. Read `AGENTS.md`.
2. Read `CODEX_HANDOFF.md`.
3. Read `docs/FROZEN_SOURCES.md`.
4. Confirm `git status --short`.
5. Confirm the latest accepted commit against the handoff.
6. Read only the frozen source sections required by the supplied current task.
7. Perform task preflight.
8. Implement only that task.
9. Commit only on complete success.
10. STOP before the next task.

Current handoff next task at time of this file:
SEC-01.

The explicit user task prompt always overrides the handoff's next-task pointer.
